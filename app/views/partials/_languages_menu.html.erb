<core-dropdown id="languages-dropdown" halign="right" valign="top" layered>
    <div relative>
        <% I18n.available_locales.each do |locale| %>
            <%= link_to image_tag(locale.to_s + '.svg') + t(locale), :locale => locale %><br>
        <% end %>
    </div>
</core-dropdown>